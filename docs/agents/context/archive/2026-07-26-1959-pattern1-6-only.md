## Current Goal

Restrict Backend person classification to `pattern1`–`pattern6`.

## Latest User Instructions

- Modify Backend so classification considers only `pattern1`–`pattern6`.
- Preserve the existing recognition flow and result schema.

## Repository State

- Verified checkout and branch: existing `main`.
- Starting HEAD remains `cf9f752abdd2`; changes are uncommitted and unpushed.
- All pre-existing Backend, Worker, documentation, and local runtime changes
  were preserved.

## Active Architecture And Contracts

- Raw and ink classification still use their respective configured gallery and
  checkpoint.
- Gallery names and prototypes are validated for full alignment, then filtered
  together by original index.
- Only `pattern1`–`pattern6` enter the similarity matrix.
- Final classification output is defensively restricted to
  `pattern1`–`pattern6` or `unassigned`.
- A gallery with no allowed candidate fails closed with
  `pattern_assets_invalid`.
- Threshold, device selection, grayscale behavior, Scanner API, Worker,
  authentication, date annotation, and artifact behavior are unchanged.

## Authoritative References And Routing

- Carried forward and updated:
  `docs/agents/prompts/frontend-polaroid-recognition-v1.md`.
  Read completely before Frontend work or API-contract review. Its authoritative
  pattern/name table now contains only `pattern1`–`pattern6`.
- Carried forward unchanged:
  `docs/windows-lan-gpu-backend-troubleshooting.md`.
  Read completely for Windows local GPU Scanner/Wrangler operation.
- No authoritative reference was removed or replaced.

## Scope And Non-Goals

- Product changes are limited to
  `backend/polaroid_recognition.py` and
  `backend/test_polaroid_recognition.py`.
- PM documentation changes update only the active pattern contract and context.
- Worker, iOS, API routes, authentication, secrets, model assets, deployment,
  and production infrastructure were not changed.
- No commit, push, or deployment was performed.

## Current Changes

- Backend gallery filtering and defensive result allowlisting.
- Backend boundary tests for excluded winners, arbitrary gallery order,
  fail-closed empty filtering, and unchanged raw/ink behavior.
- Contract mapping removal for `pattern7`, `pattern9`, `pattern10`, and
  `pattern11`.

## Completed This Window

- Implemented the six-pattern classification restriction.
- Independently reran 13 Backend tests and Python compilation.
- Completed a read-only Reviewer pass with verdict `approved`.

## Pending Work

1. Restart the running Python Backend so it loads the new source.
2. Optionally repeat the real image test and confirm no excluded pattern is
   returned.
3. Implement Frontend against the routed contract when requested.

## Subagent State

- Backend agent: completed and idle.
- Reviewer: completed with `approved` and no findings.
- No subagent is running.

## Verification State

- Backend unit tests: 13/13 passed.
- Python compilation: passed.
- `git diff --check`: passed.
- Reviewer verified index-preserving filtering, arbitrary order, excluded-class
  removal, fail-closed empty filtering, final output allowlisting, and unchanged
  raw/ink behavior.
- Live GPU runtime was not restarted or retested after this source change.

## Risks

- The currently running Backend still uses the pre-change loaded module until
  it is restarted.
- External galleries may still contain excluded entries; this is supported
  because they are filtered at load time rather than requiring asset rewrites.

## External Archive Candidates

Classifier galleries and checkpoints remain external runtime assets. Do not
copy their paths, credentials, or private runtime configuration into Git.

## Resume Prompt

Restart the Windows Python Backend from the current checkout, submit one
authenticated test through Wrangler, and confirm every returned `pattern` is
one of `pattern1`–`pattern6` or `unassigned`. Do not modify Worker, authentication,
or model assets for this verification.
